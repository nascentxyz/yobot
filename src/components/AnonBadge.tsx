import { Link as ChakraLink, Badge } from "@chakra-ui/react";

const AnonBadge = () => {
  return (
    <Badge
      h="min-content"
      my="auto !important"
      variant="solid"
      colorScheme="pink"
    >
      Metaverse
    </Badge>
  );
};

export default AnonBadge;
